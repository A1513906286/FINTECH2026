# 🔧 Fintech2026 环境配置完整指南

本文档提供详细的环境配置步骤，帮助你快速搭建 Fintech2026 开发环境。

---

## 📋 目录

1. [系统要求](#系统要求)
2. [Python 环境配置](#python-环境配置)
3. [依赖安装](#依赖安装)
4. [Ollama 配置](#ollama-配置)
5. [代理配置](#代理配置)
6. [数据库初始化](#数据库初始化)
7. [验证安装](#验证安装)
8. [常见问题](#常见问题)

---

## 系统要求

### 最低配置
- **操作系统**：Windows 10/11, macOS 10.15+, Linux (Ubuntu 18.04+)
- **CPU**：Intel i5 或同等性能
- **内存**：8GB RAM
- **硬盘**：5GB 可用空间
- **Python**：3.8 或更高版本

### 推荐配置（AI 功能）
- **CPU**：Intel i7 或 AMD Ryzen 7
- **内存**：16GB RAM
- **GPU**：NVIDIA GPU（可选，加速 Ollama）
- **硬盘**：10GB 可用空间（包含模型）

---

## Python 环境配置

### 方法一：使用 Anaconda（推荐）

#### 1. 安装 Anaconda

**Windows**：
1. 下载 [Anaconda](https://www.anaconda.com/download)
2. 运行安装程序，勾选 "Add Anaconda to PATH"
3. 完成安装

**验证安装**：
```powershell
conda --version
# 输出: conda 23.x.x
```

#### 2. 创建虚拟环境

```bash
# 创建名为 fintech 的环境，Python 3.8
conda create -n fintech python=3.8 -y

# 激活环境
conda activate fintech

# 验证 Python 版本
python --version
# 输出: Python 3.8.x
```

#### 3. 配置环境变量（Windows）

如果 `conda activate` 失败，需要初始化：
```powershell
conda init powershell
# 重启 PowerShell
```

### 方法二：使用原生 Python

#### 1. 安装 Python

**Windows**：
1. 下载 [Python 3.8+](https://www.python.org/downloads/)
2. 运行安装程序，**勾选 "Add Python to PATH"**
3. 完成安装

**验证安装**：
```powershell
python --version
# 输出: Python 3.8.x 或更高
```

#### 2. 创建虚拟环境

```bash
# 创建虚拟环境
python -m venv fintech_env

# 激活环境（Windows）
fintech_env\Scripts\activate

# 激活环境（macOS/Linux）
source fintech_env/bin/activate
```

---

## 依赖安装

### 1. 安装 dlib（重要）

**dlib 是人脸识别的核心依赖，安装较复杂，推荐使用 Conda**

#### 方法 A：使用 Conda（推荐）

```bash
conda install -c conda-forge dlib -y
```

#### 方法 B：使用 pip（Windows 需要 Visual Studio）

**Windows**：
1. 安装 [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/)
2. 勾选 "Desktop development with C++"
3. 安装后运行：
```powershell
pip install dlib
```

**macOS**：
```bash
brew install cmake
pip install dlib
```

**Linux**：
```bash
sudo apt-get install build-essential cmake
pip install dlib
```

#### 方法 C：使用预编译包（Windows）

1. 下载预编译的 `.whl` 文件：
   - [dlib-19.24.0-cp38-cp38-win_amd64.whl](https://github.com/z-mahmud22/Dlib_Windows_Python3.x)
2. 安装：
```powershell
pip install dlib-19.24.0-cp38-cp38-win_amd64.whl
```

### 2. 安装其他依赖

```bash
# 安装所有依赖
pip install -r requirements.txt

# 或手动安装
pip install flask==3.0.0
pip install face-recognition==1.3.0
pip install opencv-python==4.8.1.78
pip install pdfplumber==0.10.3
pip install xgboost==2.0.3
pip install scikit-learn==1.3.2
pip install beautifulsoup4==4.12.2
pip install requests==2.31.0
pip install Pillow==10.1.0
pip install numpy==1.24.3
pip install pandas==2.1.4
pip install joblib==1.3.2
```

### 3. 国内用户加速（可选）

```bash
# 使用清华镜像源
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# 或配置永久镜像源
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## Ollama 配置

### 1. 安装 Ollama

#### Windows

1. 访问 [Ollama 官网](https://ollama.com/download)
2. 下载 Windows 安装包
3. 运行安装程序
4. 安装完成后，Ollama 会自动启动

#### macOS

```bash
# 使用 Homebrew
brew install ollama
```

#### Linux

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### 2. 下载模型

```bash
# 下载 Llama3.2 3B 模型（推荐）
ollama pull llama3.2:3b

# 或下载更小的模型（更快，但效果稍差）
ollama pull llama3.2:1b

# 或下载量化模型（节省显存）
ollama pull llama3.2:3b-q4_0
```

### 3. 验证安装

```bash
# 查看已安装的模型
ollama list

# 应该看到类似输出：
# NAME           ID              SIZE      MODIFIED
# llama3.2:3b    a80c4f17acd5    2.0 GB    About an hour ago

# 测试模型
ollama run llama3.2:3b "你好"

# 应该看到中文回复
```

### 4. 启动 Ollama 服务

```bash
# 启动服务（保持终端窗口打开）
ollama serve

# 应该看到：
# time=2025-11-15T22:42:48.959+08:00 level=INFO msg="Listening on 127.0.0.1:11434"
```

### 5. GPU 加速（可选）

如果你有 NVIDIA GPU：

```bash
# 检查 GPU 是否被识别
ollama serve

# 日志中应该看到：
# level=INFO msg="inference compute" name=CUDA0 description="NVIDIA GeForce RTX ..."
```

---

## 代理配置

### 为什么需要代理？

国内用户访问 DuckDuckGo 可能受限，需要配置代理。

### 1. 安装代理工具

推荐使用 **Clash for Windows**：
1. 下载 [Clash for Windows](https://github.com/Fndroid/clash_for_windows_pkg/releases)
2. 安装并启动
3. 导入订阅链接
4. 启用系统代理

### 2. 配置项目代理

编辑 `app.py` 第 33-37 行：

```python
abu_dhabi_service = AbuDhabiService(
    model_name="llama3.2:3b",
    use_proxy=True,  # 启用代理
    proxy_url="http://127.0.0.1:7890"  # Clash 默认端口
)
```

### 3. 验证代理

```bash
# 测试代理是否工作
curl -x http://127.0.0.1:7890 https://duckduckgo.com

# 或在浏览器访问
# http://127.0.0.1:7890
```

### 4. 不使用代理

如果你的网络可以直接访问 DuckDuckGo：

```python
abu_dhabi_service = AbuDhabiService(
    model_name="llama3.2:3b",
    use_proxy=False,  # 禁用代理
    proxy_url=None
)
```

详细说明：[代理配置文档](./代理配置说明.md)

---

## 数据库初始化

### 1. 运行初始化脚本

```bash
python utils/init_db.py
```

### 2. 验证数据库

```bash
# 检查数据库文件是否创建
ls instance/fintech.db

# 或使用 SQLite 工具查看
sqlite3 instance/fintech.db
sqlite> .tables
# 应该看到 8 个表：
# users, cards, transactions, lottery_prizes, lottery_records, ...
```

### 3. 测试数据

初始化后会创建测试用户：
- **用户名**：Yogurt
- **用户 ID**：1
- **卡号**：5210 7132 0767 1316
- **WECoin**：200
- **抽奖次数**：10

---

## 验证安装

### 1. 检查 Python 依赖

```bash
python -c "import flask; print('Flask:', flask.__version__)"
python -c "import face_recognition; print('face_recognition: OK')"
python -c "import pdfplumber; print('pdfplumber: OK')"
python -c "import xgboost; print('XGBoost:', xgboost.__version__)"
python -c "import bs4; print('BeautifulSoup4: OK')"
```

### 2. 检查 Ollama

```bash
ollama list
ollama run llama3.2:3b "测试"
```

### 3. 启动应用

```bash
# 终端 1：启动 Ollama
ollama serve

# 终端 2：启动 Flask
python app.py
```

### 4. 访问测试页面

打开浏览器访问：
- http://127.0.0.1:5000/ - 主页
- http://127.0.0.1:5000/test_recommendations - 测试 AI 推荐

---

## 常见问题

### 1. conda 命令不可用

**问题**：`conda: command not found`

**解决**：
```powershell
# 初始化 conda
conda init powershell
# 重启终端
```

### 2. dlib 安装失败

**问题**：`ERROR: Could not build wheels for dlib`

**解决**：
```bash
# 方法 1: 使用 conda
conda install -c conda-forge dlib -y

# 方法 2: 下载预编译包（Windows）
# 访问 https://github.com/z-mahmud22/Dlib_Windows_Python3.x
```

### 3. Ollama 无法启动

**问题**：`Error: listen tcp 127.0.0.1:11434: bind: address already in use`

**解决**：
```powershell
# 端口已被占用，说明 Ollama 已在运行
# 无需重复启动

# 或杀死进程重启
taskkill /F /IM ollama.exe
ollama serve
```

### 4. 模型下载慢

**问题**：`ollama pull` 下载速度慢

**解决**：
```bash
# 使用代理下载
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
ollama pull llama3.2:3b
```

### 5. 数据库权限错误

**问题**：`sqlite3.OperationalError: unable to open database file`

**解决**：
```bash
# 创建 instance 目录
mkdir instance

# 重新初始化
python utils/init_db.py
```

---

## 下一步

✅ 环境配置完成后，请阅读：
- [README.md](../README.md) - 项目总览
- [Face_ID使用说明.md](./Face_ID使用说明.md) - 人脸识别功能
- [代理配置说明.md](./代理配置说明.md) - 代理详细配置

🚀 **开始使用**：
```bash
python app.py
```

访问 http://127.0.0.1:5000/ 开始体验！

